#!/usr/bin/env bash
set -euo pipefail

ROOT=$(cd -P "$(dirname "${BASH_SOURCE[0]}")" >/dev/null 2>&1 && pwd) \
  || { printf 'rebuild: cannot resolve repository root\n' >&2; exit 1; }

exec "$ROOT/rebuild.sh" "$@"
